/*
use crate::value_object::{talk_thread_id::TalkThreadId, talk_thread_title::TalkThreadTitle};

pub struct TalkThread {
    id: TalkThreadId,
    title: TalkThreadTitle,
}

impl TalkThread {
    pub fn new(id: TalkThreadId, title: TalkThreadTitle) -> Self {
        Self { id, title }
    }

    pub fn id(&self) -> &TalkThreadId {
        &self.id
    }

    pub fn title(&self) -> &TalkThreadTitle {
        &self.title
    }
}

impl PartialEq for TalkThread {
    fn eq(&self, other: &Self) -> bool {
        self.id == other.id
    }
}

impl Eq for TalkThread {}
*/
